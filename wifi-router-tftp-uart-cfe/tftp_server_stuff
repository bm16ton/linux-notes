Open Terminal and type;

sudo apt-get install tftpd-hpa

When asked for the directory to use as root for the TFTP server, leave the default value /srv/tftp
Move the unpacked base images files into folder /svr/tftp 
