LIST ALL IP AND MAC ADDRESSES OF CURRENTLY KNOWN NETWORK
	arp -a

FIND MAC ADDRESS OF SPECIFIC IP ADDRESS
	ping 192.168.0.56 -c 1
	arp -a

USING FING (fing looks like may be closed source)
	sudo fing 10.9.8.0/24


