udhcpd       small stand-alone busybox_version dhcp server
dhcp-probe   dhcp bootp discoverer
dhcpdump     parse dhcp packets from tcpdump
dhcping      dhcp daemon ping program
dnsmasq 	 small dns forwarder dhcp server 

